import './src/amplify';
import Routes from './src/Routes';

export default Routes;

/**
 * 4674 users;
 * Each group of 8 users gifts 500 to a user above. (Chrono order)
 * After recieving gift from below, each group of 8 users gifts 2500 to user above.
 * +
 */
